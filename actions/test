#!/bin/sh

cd ..

COMMAND='ACTION=FIND_CONTENT;SRCH_PATTERN=updateAllowed;GREP_OPTIONS= -A 1 -B 1;'
COMMAND=${COMMAND}'FNAME_PATTERN=svv-*.[p,i];FIND_IN_DIR=db;SUBDIR_LIST=*;'
COMMAND=${COMMAND}'MAX_DEPTH=5;THE_END.'
echo ${COMMAND}
echo ${COMMAND} | $(pwd)/connect
